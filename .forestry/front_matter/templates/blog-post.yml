---
label: Blog post
hide_body: false
fields:
- name: header_image
  type: file
  config:
    maxSize: 250
  label: Header image
- name: body
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: true
    schema:
      format: markdown
  label: Body
